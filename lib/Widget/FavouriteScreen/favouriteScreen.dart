import 'package:flutter/material.dart';
class FavouriteScreen extends StatelessWidget{
  Widget build(BuildContext ctx){
    return Center(
      child:Text("Your favourites will be shown here"),
    );
  }
}